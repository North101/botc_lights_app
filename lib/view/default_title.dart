import 'package:flutter/material.dart';

class DefaultTitle extends StatelessWidget {
  const DefaultTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('BotC Lights');
  }
}